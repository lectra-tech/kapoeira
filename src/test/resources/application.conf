kafka {
  bootstrap.servers = "localhost:9092"
  schema.registry.url = "http://localhost:8081"
  basic.auth.credentials.source = "USER_INFO"
  basic.auth.user.info = "srUsername:srPassword"
  consumer.group.id = "kapoeira-test"
}